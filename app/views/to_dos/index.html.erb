<h1>ToDos</h1>

<ul>
  <% @to_dos.each do |t| %>
    <li>
      <%= t.description %>
    </li>
  <% end %>
</ul>

<%= form_for ToDo.new do |f| %>
  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>
